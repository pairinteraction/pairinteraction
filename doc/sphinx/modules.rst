Modules
=======

The module ``pireal`` can be used if the Hamiltonian matrix of the considered system is purely real-valued. Otherwise, the module ``picomplex`` has to be used (e.g. if one considers electric or magnetic fields with a non-zero :math:`y`-value).

.. toctree::
   :maxdepth: 4

   pireal
   picomplex